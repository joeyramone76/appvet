This directory contains adapters for tool services used by AppVet. AppVet 
requires that registration.xml and appinfo.xml be present in this
directory.

Note that the schema defined by ToolServiceAdapter.xsd is still under
development.